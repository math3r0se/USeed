name: Linux Build

on: push
#   push:
#     branches-ignore:    
#       - 'main'
#     tags:
#       - 'v*'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: |
          make build